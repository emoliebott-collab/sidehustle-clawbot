[phases.setup]
nixPkgs = ["nodejs_24", "npm-9_x"]

[phases.install]
cmds = ["CI=false npm ci --omit=dev 2>/dev/null || CI=false npm i"]

[phases.build]
cmds = []

[start]
cmd = "node server.js"

[variables]
CI = "false"
